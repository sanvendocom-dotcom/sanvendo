{
  "name": "sanvendo-pages-r2-admin",
  "private": true,
  "version": "3.6.0",
  "description": "Sanvendo recruitment website with five-language job content, ten industry groups, R2-backed admin deletion, recruitment requests, private CV uploads, and a five-language privacy policy.",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name sanvendo",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}
