{
  "name": "sanvendo-pages-r2-admin",
  "private": true,
  "version": "3.5.1",
  "description": "Sanvendo recruitment website for Cloudflare Pages and Pages Functions.",
  "type": "module",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "build": "node scripts/verify-build.mjs",
    "dev": "npx --yes wrangler@4 pages dev public",
    "deploy": "npx --yes wrangler@4 pages deploy public --project-name sanvendo",
    "test": "node --test tests/*.test.mjs"
  }
}
