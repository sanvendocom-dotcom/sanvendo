{
  "name": "sanvendo-pages-r2-admin",
  "private": true,
  "version": "3.2.0",
  "description": "Sanvendo recruitment website with R2-backed job posts, recruitment requests, private CV uploads, and a Cloudflare Access protected admin dashboard.",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name sanvendo",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}
