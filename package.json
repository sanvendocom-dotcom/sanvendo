{
  "name": "sanvendo-pages-r2-admin",
  "private": true,
  "version": "2.0.0",
  "description": "Sanvendo recruitment website with private R2 CV upload and a Cloudflare Access protected admin dashboard.",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name sanvendo"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
